package com.pms.ipadmission.entity;

/** Billing category chosen at IP intake - distinct from PaymentMode (how an OP fee was actually collected). */
public enum AdmissionPaymentType {
    CASH,
    INSURANCE,
    CORPORATE
}
