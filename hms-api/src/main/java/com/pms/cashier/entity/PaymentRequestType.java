package com.pms.cashier.entity;

/** Request type chosen on the Patient IP Details request form (PDF Cashier Approval screen 1). */
public enum PaymentRequestType {
    ADVANCE,
    FINAL_SETTLEMENT,
    DUE_AMOUNT
}
