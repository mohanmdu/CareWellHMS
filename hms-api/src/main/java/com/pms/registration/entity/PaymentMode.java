package com.pms.registration.entity;

/** How a completed appointment's consulting fee was received. */
public enum PaymentMode {
    CASH,
    CARD,
    UPI,
    NET_BANKING,
    INSURANCE,
    OTHER
}
