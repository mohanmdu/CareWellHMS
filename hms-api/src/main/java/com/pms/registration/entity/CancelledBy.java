package com.pms.registration.entity;

/** Who initiated an appointment cancellation - mirrors the legacy screen's two distinct cancel columns/actions. */
public enum CancelledBy {
    HOSPITAL,
    PATIENT
}
